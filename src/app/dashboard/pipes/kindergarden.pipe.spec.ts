import { KindergardenPipe } from './kindergarden.pipe';

describe('KindergardenPipe', () => {
  it('create an instance', () => {
    const pipe = new KindergardenPipe();
    expect(pipe).toBeTruthy();
  });
});
